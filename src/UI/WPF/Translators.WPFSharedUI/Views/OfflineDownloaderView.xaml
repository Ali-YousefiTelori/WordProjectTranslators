<UserControl
    x:Class="Translators.UI.Views.OfflineDownloaderView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Translators.UI.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="clr-namespace:Translators.ViewModels.Pages;assembly=Translators.ViewModels"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <UserControl.Resources>
        <viewmodels:OfflineDownloaderViewModel x:Key="OfflineDownloaderViewModel" />
    </UserControl.Resources>
    <UserControl.DataContext>
        <Binding Source="{StaticResource OfflineDownloaderViewModel}"/>
    </UserControl.DataContext>
    <UserControl.Content>
        <Grid VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <TextBlock Text="{Binding Title}" Margin="0,20,0,20"/>
            <ProgressBar Value="{Binding CompletePercent}" Maximum="1" Height="30" Grid.Row="1"/>
        </Grid>
    </UserControl.Content>
</UserControl>
